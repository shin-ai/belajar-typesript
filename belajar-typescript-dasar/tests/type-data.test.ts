describe('Data Type', () => {
    it('should must declare', () => {
        let name: String = "Muhammad rifanny"
        let balance: number = 100000
        let isVip: boolean = true
        
        console.log(name)
        console.log(balance)
        console.log(isVip)

        // name = 100
        // balance = "fanny"
        // isVip = 100
    })
})